import { Text, StyleSheet, View } from 'react-native'

const styles = StyleSheet.create({

    shadow:{
        shadowColor: '#003865',
        shadowOffset:{
            width: 0,
            height: 10,
        },
        shadowOpacity: 0.25,
        shadowRadius: 3.5,
        elevation: 5
    }

})

export default styles;